export const metadata = {
  title: "PikaDraft - Draft Builder",
  description: "Draft process for building your Pokemon team",
};

export default function DraftBuildLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
