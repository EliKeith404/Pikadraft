export const metadata = {
  title: "PikaDraft - Draft",
  description: "Draft options for building your Pokemon team",
};

export default function DraftHomeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
